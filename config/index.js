/* const logger = require("morgan");
const cors = require("cors");

//Settings for the app with environment variables
module.exports = (app) => {
  app.use(logger("dev"));
  app.use(cors());
  app.use(express.json());
}; */
